# Smoke Test

update_template_recipe.py --sample-id <id> --config config.json --dry-run — prints the merged recipe size and the intended PATCH without writing. Pass when it resolves the sample, merges config, and stays <= 64 KB.
