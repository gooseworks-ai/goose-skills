# Verifier

Use:

- `../../../verifiers/video/verify-mp4-dimensions.md` — confirm 1180×2556 native and 1080×1920 export.
- `../../../verifiers/audio/verify-peak-loudness.md` — peak 0dB, mean approximately -10 to -12 LUFS.
- `../../../verifiers/package/verify-output-manifest.md` — confirm `clips/`, `edits/`, `meta-upload/` all populated.

If those shared verifiers don't exist yet, the human acceptance check in `human-test.md` is authoritative for v1.
