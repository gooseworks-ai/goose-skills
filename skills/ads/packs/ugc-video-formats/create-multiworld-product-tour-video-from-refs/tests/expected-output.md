# Expected Output

## Artifacts
- `finals/master-v2.mp4` — 720×1280, ≈27.0s, **h264** (+ aac music bed). 6 clips (3
  worlds × arrival + macro) + a brand end card.
- `working/clips/{S01..S06}.mp4` — the six Higgsfield MS `product_showcase` clips.
- `working/endcard/layer1.png` — the NB2 text-free flat-lay background.
- `working/endcard/endcard_3s.mp4` — the deterministic HTML/Playwright end card.
- `working/music/music_trimmed.mp3` — the ElevenLabs instrumental bed (27s).
- A poster still (a macro-moment frame or the end card).

## Visual shape (per world)
- **Arrival (WIDE, ~4.5s):** kinetic-calm tour of the world (slow drift / lateral track /
  soft push, ~3 ambient cuts). The environment dominates; the sealed bottle stays small,
  never a macro close-up. No whip-pans, no shake.
- Hard cut to the **Moment (top-down MACRO, ~3.5s):** the sealed product nested with its
  botanical companion; slow parallax tilt; NO hands in frame.
- Hard cut to the next world. Each world has a distinct palette + botanical.

## Close + audio
- End card (~3.0s): NB2 flat-lay background (all 3 sealed bottles + botanicals) with an
  HTML overlay — "FIND YOUR DAILY." headline, one handwritten scent label + hand-drawn
  arrow per bottle, PP serif wordmark + URL. Text is HTML-composited, never AI-rendered.
- One warm instrumental bed, energetic-but-gentle from t=0, fading the tail. **No VO, no
  captions** anywhere in the scenes.

## Non-goals
- No talking head, no dialogue, no captions during scenes (text ONLY on the end card).
- No open / cap-off / spraying bottle; no smoke/vapor/aerosol (sealed-bottle rule).
- No bottle color-coding — scent identity is carried by world palette + botanical.
- No dissolves — hard cuts (except the one whip-pan into the end card).
- No efficacy claims, no competitor jabs.
