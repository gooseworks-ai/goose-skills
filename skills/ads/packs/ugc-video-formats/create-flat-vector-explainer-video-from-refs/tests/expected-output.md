# Expected Output

## Artifacts
- `finals/master-final-30s-v1.mp4` — 1080×1920, ~30s, **h264** (+ aac stereo music). The
  full N-step routine + slate + PIL grid + CTA, word-by-word burned captions, VO-forward
  music. (Cut from the ~50s `finals/master-final.mp4`.)
- `working/silent-master.mp4` — the ANIMATED composited silent master (the cut-down source).
- `working/character-lock/creator-anchor.png` + per-scene keyframes + clean plates.
- `working/` Kling i2v clips (character scenes), the Remotion project, the PIL product grid.
- Per-scene eleven_v3 VO (+ char-timestamps) + the music bed.
- A poster still (a character-with-product frame).

## Visual shape (per beat)
- **Character scenes:** flat-vector creator-character using one product (lift spoon, press
  pump, dab eye, run hands through hair), subtle localized motion (blink/hand), the 2D
  flat-vector style held (no photoreal drift). A large corner numeral + labelled chip +
  one-line tagline + the step's real product photo composited as crisp DOM ON TOP.
- **Slate/grid/CTA scenes:** Remotion text on the rust ground — motif headline, the "N is
  enough." PIL grid of N real products, the wordmark + CTA pill + URL. No i2v.

## Close + audio
- Full-sentence eleven_v3 VO (Eryn), VO-forward over a lo-fi music bed (~-15 LUFS).
- Word-by-word burned captions, bottom-third, suppressed on the slate/grid/CTA scenes.

## Non-goals
- No talking-head UGC, no SaaS-UI mockups (this is the consumer-physical character variant).
- No baked-in text on the animated footage — all text is a Remotion DOM overlay.
- No AI-generated product grid — the closing lockup is real product photos (PIL, exact aspect).
- No keyword-fragment VO — full sentences, correct concept label ("morning routine," not "skincare").
- No frozen characters in the cut-down — sliced from the ANIMATED master, frame-diff proven.
